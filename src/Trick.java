public class Trick {
    Player[] players;
    public Trick(Player[] players) {
        this.players = players;
    }

    public void start() {
        //Get input from player
        //Check if the input is valid (ie. its following the lead card, or its a rook)
        //Compare the card they inputted
        //Give the card to the Winner's pile (Haven't wrote the pile yet, might just use hand class for it)
        //self destruct?
    }
}
